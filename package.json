{
  "name": "tiddlyharbor",
  "version": "0.2.0",
  "private": true,
  "description": "TiddlyHarbor config generation utilities",
  "scripts": {
    "setup": "node scripts/generate-config.js",
    "setup:check": "node scripts/generate-config.js --check",
    "manage": "node scripts/manage-sites.js"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
